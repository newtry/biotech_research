# 创新药行业动态追踪系统依赖

requests>=2.28.0
httpx>=0.24.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
pandas>=2.0.0
apscheduler>=3.10.0
python-dateutil>=2.8.0

# L3 反爬降级（按需启用）
# Playwright 是可选依赖：pip install playwright && playwright install chromium
# 代码里 lazy import，缺包时降级 L1/L2 不会崩